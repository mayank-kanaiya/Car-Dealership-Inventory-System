package com.incubyte.car_dealership_inventory_system.enums;

/**
 * Enum of user roles. ADMIN has elevated permissions (delete vehicles, restock).
 * USER has standard access.
 */
public enum UserRole {
    ADMIN,
    USER
}
